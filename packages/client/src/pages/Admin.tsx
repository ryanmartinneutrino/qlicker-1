/**
 * Admin page — stub placeholder.
 * TODO: Migrate from imports/ui/pages/ in the Meteor app.
 */
export default function Admin(_props: Record<string, unknown>) {
  return <div className="page">Admin</div>
}
