/**
 * Course page — stub placeholder.
 * TODO: Migrate from imports/ui/pages/ in the Meteor app.
 */
export default function Course(_props: Record<string, unknown>) {
  return <div className="page">Course</div>
}
