/**
 * Session page — stub placeholder.
 * TODO: Migrate from imports/ui/pages/ in the Meteor app.
 */
export default function Session(_props: Record<string, unknown>) {
  return <div className="page">Session</div>
}
