/**
 * Student page — stub placeholder.
 * TODO: Migrate from imports/ui/pages/ in the Meteor app.
 */
export default function Student(_props: Record<string, unknown>) {
  return <div className="page">Student</div>
}
